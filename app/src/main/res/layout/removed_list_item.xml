<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingLeft="4dp"
    android:paddingRight="4dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:layout_margin="4dp"
    android:elevation="4dp"
    android:background="?attr/colorListItem">


    <CheckBox
        android:id="@+id/removedItemCheck"
        style="@style/WhiteCheck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        android:layout_weight="0"
        android:backgroundTint="?attr/colorListItemText"
        android:buttonTint="?attr/colorListItemText" />

    <TextView
        android:id="@+id/removedItemText"
        android:layout_width="wrap_content"
        android:layout_height="26dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/description"
        android:textColor="?attr/colorListItemText"
        android:textSize="14sp" />

</LinearLayout>